# Guardrails AI — PyPI package name is guardrails-ai (NOT "guardrails").
#   pip install -r requirements.txt
#
# Wrong package (do not install):
#   pip install guardrails   # unrelated project; fails on codecov / mutmut
#
# Hub validators used by config.py must be installed separately (from repo guard/ dir):
#   guardrails hub install hub://guardrails/toxic_language
#   guardrails hub install hub://guardrails/detect_pii
#
# Run the HTTP API (do NOT use `guardrails start` — Typer/middleware bug with guardrails-api 0.4.x):
#   python run_server.py
# or:
#   ./start-api.sh

guardrails-ai>=0.10.0
guardrails-api>=0.3.0
